# MiniCLIAgent — 个人 AI 助手

从零设计并实现了一个运行在本地的个人 AI 助手，约 3000 行 Python 源码，3000+ 行测试代码。

---

## 解决了什么

现有 AI 编程工具的通病：跨会话记忆丢失、上下文窗口膨胀、工具集成割裂、复杂任务缺乏拆分机制、内部行为黑盒不可调试。MiniCLIAgent 逐个击破，从协议层到算法层全部手写。

---

## 怎么做的

**CLI 交互入口** — `run --prompt` 触发，支持单轮和交互式会话，会话自动持久化。

**Agent 运行时** — ReAct 范式主编排循环：推理、调工具、观察结果、继续推理。统一 `ToolRegistry` 作为唯一扩展点，内置文件读写、Shell 执行、任务管理等 12+ 工具，支持通过 MCP stdio 接入外部工具。

**长期记忆** — 会话压缩和退出时自动触发 LLM 总结写入 Markdown。Agent 需要时主动调用 `memory_search`，底层 BM25 关键词 + mem0 向量双路召回，加权融合排序。记忆人可读可编辑，mem0 挂了降级纯 BM25，记忆功能始终可用。

**三层上下文管理** — ① 工具结果智能裁剪，不堆放原始日志；② prompt cache TTL 过期（>5 分钟）后占位符替换旧工具结果，省 token 不破缓存；③ 历史溢出时 LLM 压缩，注入摘要并写入长期记忆。三层均保留 working memory。

**子 Agent 委托** — 中心化模式：主 Agent 拆任务、调度、汇总，子 Agent 隔离执行。相比去中心化协商，没有协调开销，不会死锁。子 Agent 就是普通工具调用，干净上下文进，结果出，多任务并行。

**Task + Worktree 闭环** — 一个任务绑定一个 Git worktree，代码修改隔离，多任务并行互不污染。

**后台任务 + 可观测** — Shell 异步执行不阻塞主循环，完整状态机。结构化日志、Transcript 落盘、Event Bus、全链路 tracing。

---

## 架构

```
CLI  → 命令解析、用户输出
App  → 组装依赖、用例流程
Core → 运行时、工具、领域逻辑
Infra → 文件系统、Shell、日志
```

**关键决策**
- 降级优先：mem0、MCP、LLM Summarizer 不可用时自动降级，主链路零中断
- Protocol 抽象而非继承，Anthropic-first 不提前过度抽象
- 不可变数据结构 + 显式依赖注入，无隐式全局状态
